package newlyGeneratedCode;
 import java.lang.*;
    class IdentifierComposite extends  {
 
protected  Object y [ ] = {
  ,  
} , y , y = <shift expression> <= <shift expression> instanceof <reference type> instanceof <reference type> <= <shift expression> & <equality expression> && <inclusive or expression>  <expression> : <conditional expression> , <variable declarator> , <variable declarator> ;
 
}